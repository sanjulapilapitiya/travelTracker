import { createApp } from 'vue'  // Import createApp from 'vue'
import 'leaflet/dist/leaflet.css'; // Import leaflet css
import App from './index.vue'       // Import the main App component

createApp(App).mount('#app')    // Create and mount the app to the #app element
